# The manifest for the "xws-subscription-api" service.
# Read the full specification for the "Backend Service" type at:
#  https://aws.github.io/copilot-cli/docs/manifest/backend-service/

# Your service name will be used in naming your resources like log groups, ECS services, etc.
name: xws-subscription-api
type: Backend Service

# Your service is reachable at "http://xws-subscription-api.${COPILOT_SERVICE_DISCOVERY_ENDPOINT}:3003" but is not public.

# Configuration for your containers and service.
image:
  # Docker build arguments. For additional overrides: https://aws.github.io/copilot-cli/docs/manifest/backend-service/#image-build
  build: Dockerfile
  # Port exposed through your container to route traffic to it.
  port: 3003

cpu: 256       # Number of CPU units for the task.
memory: 512    # Amount of memory in MiB used by the task.
count: 1       # Number of tasks that should be running in your service.
exec: true     # Enable running commands in your container.

# Optional fields for more advanced use-cases.
#
variables:
  NODE_ENV: dev
  ENV: local
  PORT: 3003
  HOST: 0.0.0.0
  CONTACT_RESOURCE: "http://xws-contact-api.flood-xws-contact.local:3000/contact"
  SUBSCRIPTION_RESOURCE: "http://xws-contact-api.flood-xws-contact.local:3000/subscription"
  PINPOINT_APPLICATION_ID: "59dd67208224475a86459036069f4766"
  AWS_REGION: "eu-west-2"

#variables:                    # Pass environment variables as key value pairs.
#  LOG_LEVEL: info

secrets:                      # Pass secrets from AWS Systems Manager (SSM) Parameter Store.
  AWS_ACCESS_KEY_ID: /flood-xws-contact/test/xws-subscription-api/AWS_ACCESS_KEY_ID
  AWS_SECRET_ACCESS_KEY: /flood-xws-contact/test/xws-subscription-api/AWS_SECRET_ACCESS_KEY


#  GITHUB_TOKEN: GITHUB_TOKEN  # The key is the name of the environment variable, the value is the name of the SSM parameter.

# You can override any of the values defined above by environment.
#environments:
#  test:
#    count: 2               # Number of tasks to run for the "test" environment.
